name: Catalog Tests
on:
  workflow_dispatch:
  push:
    paths:
      - 'src/Catalog/**'
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        dotnet-version: [ '6.0.x', '7.0.x' ]

    steps:
      - uses: actions/checkout@v2
      - name: Setup .NET SDK ${{ matrix.dotnet-version }}
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: ${{ matrix.dotnet-version }}
      - name: Display dotnet version
        run: dotnet --version
      - name: Install dependencies
        working-directory: ./src/Services/Catalog
        run: dotnet restore --solution src/Catalog/Catalog.sln --no-cache
      - name: Build
        working-directory: ./src/Services/Catalog
        run: dotnet build --configuration Release --no-restore
      - name: Test
        working-directory: ./src/Services/Catalog
        run: dotnet test --configuration Release --no-restore --verbosity normal --project
      - name: Upload dotnet test results
        uses: actions/upload-artifact@v3
        with:
          name: dotnet-test-results-${{ matrix.dotnet-version }}
          path: TestResults-${{ matrix.dotnet-version }}
        if: ${{ always() }}
